# Venus Workshop CRM

## 📌 О проекте

**Venus Workshop** — это CRM-приложение на базе **Google Apps Script + HTML/CSS/JS**, предназначенное для управления мастер-классами, расходами, аналитикой и календарём бронирований. Проект оптимизирован под **Telegram WebApp** и мобильное использование.

Проект развивается модульно: каждая крупная функция (МК, Расходы, Аналитика, Календарь) изолирована логически и может масштабироваться без переписывания ядра.

---

## 🚀 Основные возможности

* 📦 Учёт мастер-классов (доходы)
* 🧾 Учёт расходов
* 📊 Аналитика (выручка, прибыль, посещаемость)
* 📅 **Календарь бронирований мастер-классов**
* 🔄 Автоматическая связь календаря с доходами
* 🌗 Поддержка светлой / тёмной темы
* 📱 Mobile-first + Telegram WebApp UX

---

## 🧭 Архитектура проекта

```
├── Code.gs        # Backed (Google Apps Script)
├── Index.html     # UI (HTML + CSS + JS)
├── README.md      # Документация проекта
```

### Backend (Google Apps Script)

* Работает как API (doPost)
* Использует Google Sheets как базу данных
* Все методы изолированы и вызываются через `action`

### Frontend

* Чистый HTML / CSS / JS
* Без сторонних UI-фреймворков
* Анимации, свайпы, haptic feedback
* Адаптация под мобильные устройства

---

## 🗂 Структура Google Sheets (БД)

### 1️⃣ `Мастер Классы`

Хранит доходы (включая автоматически созданные записи из календаря)

| Колонка  | Описание           |
| -------- | ------------------ |
| Дата     | Дата операции      |
| Название | Название МК        |
| Кол-во   | Количество человек |
| Цена     | Цена за 1 человека |
| Итого    | Общая сумма        |

---

### 2️⃣ `Расходные материалы`

| Колонка  | Описание     |
| -------- | ------------ |
| Дата     | Дата расхода |
| Название | Наименование |
| Цена     | Цена         |
| Кол-во   | Количество   |
| Итого    | Сумма        |

---

### 3️⃣ `Справочник`

Используется для автодополнения

| Доходы      | Расходы          |
| ----------- | ---------------- |
| Название МК | Название расхода |

---

### 4️⃣ `Календарь_Брони` (ключевой лист)

| Колонка         | Тип      | Описание                  |
| --------------- | -------- | ------------------------- |
| ID              | string   | Уникальный идентификатор  |
| Дата            | date     | Дата мастер-класса        |
| Время_Начала    | time     | Начало                    |
| Время_Окончания | time     | Конец                     |
| Название        | string   | Название МК               |
| Цена            | number   | Цена за человека          |
| Количество      | number   | Кол-во участников         |
| Сумма           | number   | Цена × Кол-во             |
| Предоплата      | number   | Может быть 0              |
| Статус          | enum     | planned / done / canceled |
| Дата_Создания   | datetime | Когда создано             |

---

## 📅 Логика календаря

### Цветовая индикация загрузки

| МК в день | Отображение      |
| --------- | ---------------- |
| 0         | Просто число     |
| 1         | Лёгкий акцент    |
| 2–3       | Более насыщенный |
| 4+        | Мягкий красный   |

---

### Статусы мастер-классов

#### ✅ Отработан (`done`)

* Автоматически создаёт запись в **Мастер Классы**
* Сумма = итоговая сумма МК

#### ❌ Отмена (`canceled`)

* Если есть предоплата → она добавляется в доходы
* Если предоплаты нет → доход не создаётся

---

## 🔌 Backend API (GAS)

### Основные методы

```js
getInitialConfig()
getTableData(type)
saveTransaction(data)
editTransaction(data)
deleteTransaction(data)
getAnalyticsData(year, month)
```

### Методы календаря

```js
getCalendarData(year, month)
getBookingsByDate(date)
addBooking(data)
updateBooking(id, data)
changeBookingStatus(id, status)
```

Все методы вызываются через `doPost` по полю `action`.

---

## 🧠 Принципы проекта

* ❌ Никаких «магических» зависимостей
* ✅ Предсказуемая логика
* 🧩 Модульность
* 📐 Чистая архитектура
* 📱 Mobile-first

Проект спроектирован так, чтобы:

* легко масштабироваться
* подключать Telegram-бота
* добавлять онлайн-запись

---

## 🔮 Вектор развития

* 🤖 Telegram-бот для клиентов
* 🌐 Публичная страница записи
* 🔐 Роли пользователей
* 📤 Экспорт отчётов
* 📈 Расширенная аналитика

---

## 🛠 Разработка

Рекомендуемый подход:

1. Backend → данные
2. UI → визуал
3. Связь → логика
4. Тест → стабильность

---

## 🧠 Работа с Codex и Google Apps Script (GAS)

### Почему это важно

GPT Codex **не имеет прямого доступа** к коду, который хранится внутри Google Apps Script (GAS). Codex работает **только с файлами, доступными в репозитории**, либо с кодом, явно переданным ему в задаче.

Поэтому для корректной и безопасной разработки используется принцип **зеркала кода**.

---

### 🔁 Принцип «Зеркало → Продакшн»

Репозиторий является **источником разработки**, а Google Apps Script — **средой исполнения**.

```
Repo (Codex)
   ↓
Code.gs (зеркало)
   ↓
Google Apps Script (продакшн)
```

* Codex **читает и изменяет код только в репозитории**
* Google Таблица получает изменения **вручную**, после проверки
* Репозиторий всегда содержит **актуальную версию логики**

---

### 📂 Структура для Codex

Даже если backend физически находится в Google Таблице, в репозитории **обязательно** должен присутствовать файл:

```
Code.gs
```

В начале файла рекомендуется размещать комментарий:

```js
/**
 * ⚠ SOURCE OF TRUTH
 * Этот файл является зеркалом Google Apps Script (Code.gs).
 * Все изменения сначала разрабатываются и проверяются через Codex,
 * затем вручную переносятся в Google Apps Script.
 */
```

---

### 🧪 Рекомендуемый рабочий процесс

1. Изменения разрабатываются через **GPT Codex**
2. Codex работает с `Code.gs` и `Index.html` в репозитории
3. Разработчик копирует изменения в Google Apps Script
4. Производится тестирование в таблице
5. После подтверждения — коммит в репозиторий

Такой подход:

* предотвращает поломку продакшна
* упрощает откат изменений
* позволяет масштабировать проект

---

### 🚫 Чего делать не стоит

* ❌ Разрабатывать логику только в Google Apps Script без зеркала
* ❌ Давать Codex частичный код backend
* ❌ Менять имена функций без синхронизации frontend ↔ backend

Это приводит к расхождению логики и трудноуловимым ошибкам.

---

### 🔮 На будущее

Такая схема полностью совместима с:

* `clasp` (синхронизация GAS ↔ GitHub)
* выносом backend в отдельный API
* командной разработкой

Проект изначально готов к росту без архитектурных переделок.

---

## ✨ Финал

Этот репозиторий — **живая система**, а не просто код.

> Хороший календарь экономит время.
> Отличный — приносит деньги.
